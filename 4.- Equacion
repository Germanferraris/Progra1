// cuatro.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream> 
#include "conio.h"
#include "math.h"
using namespace std;
void main()
{
	int n,s;
	float m;
	cout<<"ingresar el valor de n"<<endl;
	cin>>n;
	cout<<"ingresar el valor de x";
	cin>>x;
	s=(((8+8+(n-1)*((pow(i,n)/(n+1))*n)/2)*(pow(i,n)/(n+1))*n)));
	cout<<"la suma es: "<<suma<<endl;
	getch();
 
}
